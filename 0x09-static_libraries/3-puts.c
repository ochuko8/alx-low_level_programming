#include "main.h"
#include<stdio.h>
#include<string.h>
/**
 * _puts - this function returns 0
 *@str: str is a parameter
 * Return: Always 0 (Success)
 */
void _puts(char *str);
{
	_puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
